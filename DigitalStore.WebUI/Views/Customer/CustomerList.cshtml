@using DigitalStore.Models
@using DigitalStore.WebUI.ExtensionClasses
@using ReflectionIT.Mvc.Paging
@model ReflectionIT.Mvc.Paging.PagingList<Customer>
@addTagHelper *, ReflectionIT.Mvc.Paging

@{
    Layout = "../Shared/_PersonLayout";
}

<nav class="pagination">
    <vc:pager paging-list="@Model"/>
</nav>

<div class="container p-3">
<table class="table table-bordered table-striped" style="width:100%">
	<div class="col-12 text-end">
			<a asp-controller="Customer" asp-action="Create" class="btn btn-primary" style="width:100px">
				<i class="bi bi-plus-square"></i> &nbsp; Create
			</a>
	</div>
	<thead>
		<tr>
			<th>
				Id
			</th>
			<th>
				First Name
			</th>
			<th>
				LastName
			</th>
			<th>
				Mid Name
			</th>
			<th>
				Phone Number
			</th>
			<th>
				Email
			</th>
			<th>
				City
			</th>
			<th></th>
		</tr>
	</thead>
	<tbody>
		@foreach (var obj in Model)
		{
			<tr>
				<td width="5%">
					@obj.Id
				</td>
				<td width="10%">
					@obj.FirstName
				</td>
				<td width="10%">
					@obj.LastName
				</td>
				<td width="7%">
					@obj.MidName
				</td>
				<td width="10%">
					@obj.PhoneNumber
				</td>
				<td width="10%">
					@obj.Email
				</td>
				<td width="10%">
					@obj.City.CityName
				</td>
				<td>
					<div class="w-100 btn-group" role="group">
						<a asp-controller="Customer" asp-action="Edit" asp-route-id="@obj.Id" class="btn btn-primary mx-2" style="width:50px">
							<i class="bi bi-pencil"></i>Edit</a>
							<a asp-controller="Customer" asp-action="Delete" asp-route-id="@obj.Id" class="btn btn-danger mx-2" style="width:80px">
							Delete</a>
					</div>
				</td>
			</tr>
		}
	</tbody>
</table>
</div>

<nav class="pagination">
    <vc:pager paging-list="@Model"/>
</nav>